scalar Time
scalar Upload

schema {
  query: Query
  mutation: Mutation
}

enum PermissionAbility {
  CREATE
  UPDATE
  DELETE
  READ
  EXECUTE
}

directive @access(
  requiredProjectId: Boolean
  subject: String
  action: PermissionAbility
) on FIELD_DEFINITION
