{
  "name": "cdr-intelligence",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prisma:primary:validate": "prisma validate --schema=prisma/primary",
    "prisma:primary:generate": "prisma generate --schema=prisma/primary",
    "prisma:primary:migrate": "prisma migrate dev --schema=prisma/primary",
    "prisma:primary:studio": "prisma studio --schema=prisma/primary",
    "prisma:primary:db-push": "prisma db push --schema=prisma/primary",
    "prisma:primary:db-pull": "prisma db pull --schema=prisma/primary",
    "prisma:primary:reset": "prisma migrate reset --schema=prisma/primary"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "22.10.2",
    "prisma": "6.1.0",
    "tsx": "4.19.2",
    "typescript": "5.7.2"
  },
  "dependencies": {
    "@prisma/client": "6.1.0"
  }
}
