type Authentication {
  accountId: ID!
  # account: Account
  projectIds: [ID!]
  token: String!
  refreshToken: String!
}

type Mutation {
  login(username: String!, password: String!): Authentication!
}
